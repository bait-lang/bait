// SPDX-FileCopyrightText: Lukas Neubert
// SPDX-License-Identifier: MIT

// TODO C enum.str()
enum Color{
	red
	green
	blue
}

fun test_enum() {
	c := Color.red
	assert c.str() == 'red'

	c2 := Color.blue
	assert c2.str() == 'blue'
}
