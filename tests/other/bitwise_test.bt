// SPDX-FileCopyrightText: Lukas Neubert <lukas.neubert@proton.me>
// SPDX-License-Identifier: MIT

// FIXME js backend: bitwise not on u32 should stay in u32 bounds.
fun test_unsigned_not() {
	zero := 0 as u32
	one := 1 as u32

	assert ~zero == MAX_U32
	assert ~one == (MAX_U32 - 1)
}
