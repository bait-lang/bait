Usage:
  bait run [options] <file.bt|directory>

This command is like running `bait build` and executing the compiled exe.
However it is deleted afterwards.

For supported run options, see `bait help build`.
